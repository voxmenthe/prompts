---
description: Running terminal commands
globs: 
alwaysApply: false
---
* our venv for the backend is ~/venvs/opt3132 so may need to be activated before running some things such as tests
* it may already be activated though
* An example of activating and running (just an example): `source ~/venvs/opt3132/bin/activate && cd src/tests && python -m pytest`