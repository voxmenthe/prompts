#!/usr/bin/env bash
# ------------------------------------------------------------------
#  Global Claude interceptor: block raw “pip” calls
# ------------------------------------------------------------------

cat <<'MSG'
❌  Direct invocation of pip is disabled.
   Prefer `uv add <package>` instead.
   If absolutely necessary, use uv’s drop‑in replacement instead:
       uv pip <args…>
MSG
exit 1
