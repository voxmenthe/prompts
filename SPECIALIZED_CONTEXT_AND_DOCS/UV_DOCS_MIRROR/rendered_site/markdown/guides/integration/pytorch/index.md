# [Using uv with PyTorch](#using-uv-with-pytorch)

The [PyTorch](https://pytorch.org/)ecosystem is a popular choice for deep learning research and development. You can use uv to manage PyTorch projects and PyTorch dependencies across different Python versions and environments, even controlling for the choice of accelerator (e.g., CPU-only vs. CUDA). 

!!! note "Note"

    Some of the features outlined in this guide require uv version 0.5.3 or later. We recommend upgrading prior to configuring PyTorch. 

## [Installing PyTorch](#installing-pytorch)

From a packaging perspective, PyTorch has a few uncommon characteristics: 

- Many PyTorch wheels are hosted on a dedicated index, rather than the Python Package Index (PyPI). As such, installing PyTorch often requires configuring a project to use the PyTorch index. 

- PyTorch produces distinct builds for each accelerator (e.g., CPU-only, CUDA). Since there's no standardized mechanism for specifying these accelerators when publishing or installing, PyTorch encodes them in the local version specifier. As such, PyTorch versions will often look like `2.5.1+cpu `, `2.5.1+cu121 `, etc. 

- Builds for different accelerators are published to different indexes. For example, the `+cpu `builds are published on [https://download.pytorch.org/whl/cpu](https://download.pytorch.org/whl/cpu), while the `+cu121 `builds are published on [https://download.pytorch.org/whl/cu121](https://download.pytorch.org/whl/cu121). 

As such, the necessary packaging configuration will vary depending on both the platforms you need to support and the accelerators you want to enable. 

To start, consider the following (default) configuration, which would be generated by running `uv init --python 3.14 `followed by `uv add torch torchvision `. 

In this case, PyTorch would be installed from PyPI, which hosts CPU-only wheels for Windows and macOS, and GPU-accelerated wheels on Linux (targeting CUDA 12.8, as of PyTorch 2.9.1): 

```
[#__codelineno-0-1](#__codelineno-0-1)[project]
[#__codelineno-0-2](#__codelineno-0-2)name = "project"
[#__codelineno-0-3](#__codelineno-0-3)version = "0.1.0"
[#__codelineno-0-4](#__codelineno-0-4)requires-python = ">=3.14"
[#__codelineno-0-5](#__codelineno-0-5)dependencies = [
[#__codelineno-0-6](#__codelineno-0-6)  "torch>=2.9.1",
[#__codelineno-0-7](#__codelineno-0-7)  "torchvision>=0.24.1",
[#__codelineno-0-8](#__codelineno-0-8)]

```

This is a valid configuration for projects that want to use CPU builds on Windows and macOS, and CUDA-enabled builds on Linux. However, if you need to support different platforms or accelerators, you'll need to configure the project accordingly. 

## [Using a PyTorch index](#using-a-pytorch-index)

In some cases, you may want to use a specific PyTorch variant across all platforms. For example, you may want to use the CPU-only builds on Linux too. 

In such cases, the first step is to add the relevant PyTorch index to your `pyproject.toml `: 

=== "CPU-only"

```
[#__codelineno-1-1](#__codelineno-1-1)[[tool.uv.index]]
[#__codelineno-1-2](#__codelineno-1-2)name = "pytorch-cpu"
[#__codelineno-1-3](#__codelineno-1-3)url = "https://download.pytorch.org/whl/cpu"
[#__codelineno-1-4](#__codelineno-1-4)explicit = true

```

=== "CUDA 11.8"

```
[#__codelineno-2-1](#__codelineno-2-1)[[tool.uv.index]]
[#__codelineno-2-2](#__codelineno-2-2)name = "pytorch-cu118"
[#__codelineno-2-3](#__codelineno-2-3)url = "https://download.pytorch.org/whl/cu118"
[#__codelineno-2-4](#__codelineno-2-4)explicit = true

```

=== "CUDA 12.6"

```
[#__codelineno-3-1](#__codelineno-3-1)[[tool.uv.index]]
[#__codelineno-3-2](#__codelineno-3-2)name = "pytorch-cu126"
[#__codelineno-3-3](#__codelineno-3-3)url = "https://download.pytorch.org/whl/cu126"
[#__codelineno-3-4](#__codelineno-3-4)explicit = true

```

=== "CUDA 12.8"

```
[#__codelineno-4-1](#__codelineno-4-1)[[tool.uv.index]]
[#__codelineno-4-2](#__codelineno-4-2)name = "pytorch-cu128"
[#__codelineno-4-3](#__codelineno-4-3)url = "https://download.pytorch.org/whl/cu128"
[#__codelineno-4-4](#__codelineno-4-4)explicit = true

```

=== "CUDA 13.0"

```
[#__codelineno-5-1](#__codelineno-5-1)[[tool.uv.index]]
[#__codelineno-5-2](#__codelineno-5-2)name = "pytorch-cu130"
[#__codelineno-5-3](#__codelineno-5-3)url = "https://download.pytorch.org/whl/cu130"
[#__codelineno-5-4](#__codelineno-5-4)explicit = true

```

=== "ROCm6"

```
[#__codelineno-6-1](#__codelineno-6-1)[[tool.uv.index]]
[#__codelineno-6-2](#__codelineno-6-2)name = "pytorch-rocm"
[#__codelineno-6-3](#__codelineno-6-3)url = "https://download.pytorch.org/whl/rocm6.4"
[#__codelineno-6-4](#__codelineno-6-4)explicit = true

```

=== "Intel GPUs"

```
[#__codelineno-7-1](#__codelineno-7-1)[[tool.uv.index]]
[#__codelineno-7-2](#__codelineno-7-2)name = "pytorch-xpu"
[#__codelineno-7-3](#__codelineno-7-3)url = "https://download.pytorch.org/whl/xpu"
[#__codelineno-7-4](#__codelineno-7-4)explicit = true

```

We recommend the use of `explicit = true `to ensure that the index is _only _used for `torch `, `torchvision `, and other PyTorch-related packages, as opposed to generic dependencies like `jinja2 `, which should continue to be sourced from the default index (PyPI). 

Next, update the `pyproject.toml `to point `torch `and `torchvision `to the desired index: 

=== "CPU-only"

```
[#__codelineno-8-1](#__codelineno-8-1)[tool.uv.sources]
[#__codelineno-8-2](#__codelineno-8-2)torch = [
[#__codelineno-8-3](#__codelineno-8-3)  { index = "pytorch-cpu" },
[#__codelineno-8-4](#__codelineno-8-4)]
[#__codelineno-8-5](#__codelineno-8-5)torchvision = [
[#__codelineno-8-6](#__codelineno-8-6)  { index = "pytorch-cpu" },
[#__codelineno-8-7](#__codelineno-8-7)]

```

=== "CUDA 11.8"

PyTorch doesn't publish CUDA builds for macOS. As such, we gate on `sys_platform `to instruct uv to use the PyTorch index on Linux and Windows, but fall back to PyPI on macOS: 

```
[#__codelineno-9-1](#__codelineno-9-1)[tool.uv.sources]
[#__codelineno-9-2](#__codelineno-9-2)torch = [
[#__codelineno-9-3](#__codelineno-9-3)  { index = "pytorch-cu118", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-9-4](#__codelineno-9-4)]
[#__codelineno-9-5](#__codelineno-9-5)torchvision = [
[#__codelineno-9-6](#__codelineno-9-6)  { index = "pytorch-cu118", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-9-7](#__codelineno-9-7)]

```

=== "CUDA 12.6"

PyTorch doesn't publish CUDA builds for macOS. As such, we gate on `sys_platform `to instruct uv to limit the PyTorch index to Linux and Windows, falling back to PyPI on macOS: 

```
[#__codelineno-10-1](#__codelineno-10-1)[tool.uv.sources]
[#__codelineno-10-2](#__codelineno-10-2)torch = [
[#__codelineno-10-3](#__codelineno-10-3)  { index = "pytorch-cu126", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-10-4](#__codelineno-10-4)]
[#__codelineno-10-5](#__codelineno-10-5)torchvision = [
[#__codelineno-10-6](#__codelineno-10-6)  { index = "pytorch-cu126", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-10-7](#__codelineno-10-7)]

```

=== "CUDA 12.8"

PyTorch doesn't publish CUDA builds for macOS. As such, we gate on `sys_platform `to instruct uv to limit the PyTorch index to Linux and Windows, falling back to PyPI on macOS: 

```
[#__codelineno-11-1](#__codelineno-11-1)[tool.uv.sources]
[#__codelineno-11-2](#__codelineno-11-2)torch = [
[#__codelineno-11-3](#__codelineno-11-3)  { index = "pytorch-cu128", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-11-4](#__codelineno-11-4)]
[#__codelineno-11-5](#__codelineno-11-5)torchvision = [
[#__codelineno-11-6](#__codelineno-11-6)  { index = "pytorch-cu128", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-11-7](#__codelineno-11-7)]

```

=== "CUDA 13.0"

PyTorch doesn't publish CUDA builds for macOS. As such, we gate on `sys_platform `to instruct uv to limit the PyTorch index to Linux and Windows, falling back to PyPI on macOS: 

```
[#__codelineno-12-1](#__codelineno-12-1)[tool.uv.sources]
[#__codelineno-12-2](#__codelineno-12-2)torch = [
[#__codelineno-12-3](#__codelineno-12-3)  { index = "pytorch-cu130", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-12-4](#__codelineno-12-4)]
[#__codelineno-12-5](#__codelineno-12-5)torchvision = [
[#__codelineno-12-6](#__codelineno-12-6)  { index = "pytorch-cu130", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-12-7](#__codelineno-12-7)]

```

=== "ROCm6"

PyTorch doesn't publish ROCm6 builds for macOS or Windows. As such, we gate on `sys_platform `to instruct uv to limit the PyTorch index to Linux, falling back to PyPI on macOS and Windows: 

```
[#__codelineno-13-1](#__codelineno-13-1)[tool.uv.sources]
[#__codelineno-13-2](#__codelineno-13-2)torch = [
[#__codelineno-13-3](#__codelineno-13-3)  { index = "pytorch-rocm", marker = "sys_platform == 'linux'" },
[#__codelineno-13-4](#__codelineno-13-4)]
[#__codelineno-13-5](#__codelineno-13-5)torchvision = [
[#__codelineno-13-6](#__codelineno-13-6)  { index = "pytorch-rocm", marker = "sys_platform == 'linux'" },
[#__codelineno-13-7](#__codelineno-13-7)]
[#__codelineno-13-8](#__codelineno-13-8)# ROCm6 support relies on `pytorch-triton-rocm`, which should also be installed from the PyTorch index
[#__codelineno-13-9](#__codelineno-13-9)# (and included in `project.dependencies`).
[#__codelineno-13-10](#__codelineno-13-10)pytorch-triton-rocm = [
[#__codelineno-13-11](#__codelineno-13-11)  { index = "pytorch-rocm", marker = "sys_platform == 'linux'" },
[#__codelineno-13-12](#__codelineno-13-12)]

```

=== "Intel GPUs"

PyTorch doesn't publish Intel GPU builds for macOS. As such, we gate on `sys_platform `to instruct uv to limit the PyTorch index to Linux and Windows, falling back to PyPI on macOS: 

```
[#__codelineno-14-1](#__codelineno-14-1)[tool.uv.sources]
[#__codelineno-14-2](#__codelineno-14-2)torch = [
[#__codelineno-14-3](#__codelineno-14-3)  { index = "pytorch-xpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-14-4](#__codelineno-14-4)]
[#__codelineno-14-5](#__codelineno-14-5)torchvision = [
[#__codelineno-14-6](#__codelineno-14-6)  { index = "pytorch-xpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-14-7](#__codelineno-14-7)]
[#__codelineno-14-8](#__codelineno-14-8)# Intel GPU support relies on `pytorch-triton-xpu`, which should also be installed from the PyTorch index
[#__codelineno-14-9](#__codelineno-14-9)# (and included in `project.dependencies`).
[#__codelineno-14-10](#__codelineno-14-10)pytorch-triton-xpu = [
[#__codelineno-14-11](#__codelineno-14-11)  { index = "pytorch-xpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
[#__codelineno-14-12](#__codelineno-14-12)]

```

As a complete example, the following project would use PyTorch's CPU-only builds on all platforms: 

```
[#__codelineno-15-1](#__codelineno-15-1)[project]
[#__codelineno-15-2](#__codelineno-15-2)name = "project"
[#__codelineno-15-3](#__codelineno-15-3)version = "0.1.0"
[#__codelineno-15-4](#__codelineno-15-4)requires-python = ">=3.14.0"
[#__codelineno-15-5](#__codelineno-15-5)dependencies = [
[#__codelineno-15-6](#__codelineno-15-6)  "torch>=2.9.1",
[#__codelineno-15-7](#__codelineno-15-7)  "torchvision>=0.24.1",
[#__codelineno-15-8](#__codelineno-15-8)]
[#__codelineno-15-9](#__codelineno-15-9)
[#__codelineno-15-10](#__codelineno-15-10)[tool.uv.sources]
[#__codelineno-15-11](#__codelineno-15-11)torch = [
[#__codelineno-15-12](#__codelineno-15-12)    { index = "pytorch-cpu" },
[#__codelineno-15-13](#__codelineno-15-13)]
[#__codelineno-15-14](#__codelineno-15-14)torchvision = [
[#__codelineno-15-15](#__codelineno-15-15)    { index = "pytorch-cpu" },
[#__codelineno-15-16](#__codelineno-15-16)]
[#__codelineno-15-17](#__codelineno-15-17)
[#__codelineno-15-18](#__codelineno-15-18)[[tool.uv.index]]
[#__codelineno-15-19](#__codelineno-15-19)name = "pytorch-cpu"
[#__codelineno-15-20](#__codelineno-15-20)url = "https://download.pytorch.org/whl/cpu"
[#__codelineno-15-21](#__codelineno-15-21)explicit = true

```

## [Configuring accelerators with environment markers](#configuring-accelerators-with-environment-markers)

In some cases, you may want to use CPU-only builds in one environment (e.g., macOS and Windows), and CUDA-enabled builds in another (e.g., Linux). 

With `tool.uv.sources `, you can use environment markers to specify the desired index for each platform. For example, the following configuration would use PyTorch's CUDA-enabled builds on Linux, and CPU-only builds on all other platforms (e.g., macOS and Windows): 

```
[#__codelineno-16-1](#__codelineno-16-1)[project]
[#__codelineno-16-2](#__codelineno-16-2)name = "project"
[#__codelineno-16-3](#__codelineno-16-3)version = "0.1.0"
[#__codelineno-16-4](#__codelineno-16-4)requires-python = ">=3.14.0"
[#__codelineno-16-5](#__codelineno-16-5)dependencies = [
[#__codelineno-16-6](#__codelineno-16-6)  "torch>=2.9.1",
[#__codelineno-16-7](#__codelineno-16-7)  "torchvision>=0.24.1",
[#__codelineno-16-8](#__codelineno-16-8)]
[#__codelineno-16-9](#__codelineno-16-9)
[#__codelineno-16-10](#__codelineno-16-10)[tool.uv.sources]
[#__codelineno-16-11](#__codelineno-16-11)torch = [
[#__codelineno-16-12](#__codelineno-16-12)  { index = "pytorch-cpu", marker = "sys_platform != 'linux'" },
[#__codelineno-16-13](#__codelineno-16-13)  { index = "pytorch-cu128", marker = "sys_platform == 'linux'" },
[#__codelineno-16-14](#__codelineno-16-14)]
[#__codelineno-16-15](#__codelineno-16-15)torchvision = [
[#__codelineno-16-16](#__codelineno-16-16)  { index = "pytorch-cpu", marker = "sys_platform != 'linux'" },
[#__codelineno-16-17](#__codelineno-16-17)  { index = "pytorch-cu128", marker = "sys_platform == 'linux'" },
[#__codelineno-16-18](#__codelineno-16-18)]
[#__codelineno-16-19](#__codelineno-16-19)
[#__codelineno-16-20](#__codelineno-16-20)[[tool.uv.index]]
[#__codelineno-16-21](#__codelineno-16-21)name = "pytorch-cpu"
[#__codelineno-16-22](#__codelineno-16-22)url = "https://download.pytorch.org/whl/cpu"
[#__codelineno-16-23](#__codelineno-16-23)explicit = true
[#__codelineno-16-24](#__codelineno-16-24)
[#__codelineno-16-25](#__codelineno-16-25)[[tool.uv.index]]
[#__codelineno-16-26](#__codelineno-16-26)name = "pytorch-cu128"
[#__codelineno-16-27](#__codelineno-16-27)url = "https://download.pytorch.org/whl/cu128"
[#__codelineno-16-28](#__codelineno-16-28)explicit = true

```

Similarly, the following configuration would use PyTorch's AMD GPU builds on Linux, and CPU-only builds on Windows and macOS (by way of falling back to PyPI): 

```
[#__codelineno-17-1](#__codelineno-17-1)[project]
[#__codelineno-17-2](#__codelineno-17-2)name = "project"
[#__codelineno-17-3](#__codelineno-17-3)version = "0.1.0"
[#__codelineno-17-4](#__codelineno-17-4)requires-python = ">=3.14.0"
[#__codelineno-17-5](#__codelineno-17-5)dependencies = [
[#__codelineno-17-6](#__codelineno-17-6)  "torch>=2.9.1",
[#__codelineno-17-7](#__codelineno-17-7)  "torchvision>=0.24.1",
[#__codelineno-17-8](#__codelineno-17-8)  "pytorch-triton-rocm>=3.5.1 ; sys_platform == 'linux'",
[#__codelineno-17-9](#__codelineno-17-9)]
[#__codelineno-17-10](#__codelineno-17-10)
[#__codelineno-17-11](#__codelineno-17-11)[tool.uv.sources]
[#__codelineno-17-12](#__codelineno-17-12)torch = [
[#__codelineno-17-13](#__codelineno-17-13)  { index = "pytorch-rocm", marker = "sys_platform == 'linux'" },
[#__codelineno-17-14](#__codelineno-17-14)]
[#__codelineno-17-15](#__codelineno-17-15)torchvision = [
[#__codelineno-17-16](#__codelineno-17-16)  { index = "pytorch-rocm", marker = "sys_platform == 'linux'" },
[#__codelineno-17-17](#__codelineno-17-17)]
[#__codelineno-17-18](#__codelineno-17-18)pytorch-triton-rocm = [
[#__codelineno-17-19](#__codelineno-17-19)  { index = "pytorch-rocm", marker = "sys_platform == 'linux'" },
[#__codelineno-17-20](#__codelineno-17-20)]
[#__codelineno-17-21](#__codelineno-17-21)
[#__codelineno-17-22](#__codelineno-17-22)[[tool.uv.index]]
[#__codelineno-17-23](#__codelineno-17-23)name = "pytorch-rocm"
[#__codelineno-17-24](#__codelineno-17-24)url = "https://download.pytorch.org/whl/rocm6.4"
[#__codelineno-17-25](#__codelineno-17-25)explicit = true

```

Or, for Intel GPU builds: 

```
[#__codelineno-18-1](#__codelineno-18-1)[project]
[#__codelineno-18-2](#__codelineno-18-2)name = "project"
[#__codelineno-18-3](#__codelineno-18-3)version = "0.1.0"
[#__codelineno-18-4](#__codelineno-18-4)requires-python = ">=3.14.0"
[#__codelineno-18-5](#__codelineno-18-5)dependencies = [
[#__codelineno-18-6](#__codelineno-18-6)  "torch>=2.9.1",
[#__codelineno-18-7](#__codelineno-18-7)  "torchvision>=0.24.1",
[#__codelineno-18-8](#__codelineno-18-8)  "pytorch-triton-xpu>=3.5.0 ; sys_platform == 'win32' or sys_platform == 'linux'",
[#__codelineno-18-9](#__codelineno-18-9)]
[#__codelineno-18-10](#__codelineno-18-10)
[#__codelineno-18-11](#__codelineno-18-11)[tool.uv.sources]
[#__codelineno-18-12](#__codelineno-18-12)torch = [
[#__codelineno-18-13](#__codelineno-18-13)  { index = "pytorch-xpu", marker = "sys_platform == 'win32' or sys_platform == 'linux'" },
[#__codelineno-18-14](#__codelineno-18-14)]
[#__codelineno-18-15](#__codelineno-18-15)torchvision = [
[#__codelineno-18-16](#__codelineno-18-16)  { index = "pytorch-xpu", marker = "sys_platform == 'win32' or sys_platform == 'linux'" },
[#__codelineno-18-17](#__codelineno-18-17)]
[#__codelineno-18-18](#__codelineno-18-18)pytorch-triton-xpu = [
[#__codelineno-18-19](#__codelineno-18-19)  { index = "pytorch-xpu", marker = "sys_platform == 'win32' or sys_platform == 'linux'" },
[#__codelineno-18-20](#__codelineno-18-20)]
[#__codelineno-18-21](#__codelineno-18-21)
[#__codelineno-18-22](#__codelineno-18-22)[[tool.uv.index]]
[#__codelineno-18-23](#__codelineno-18-23)name = "pytorch-xpu"
[#__codelineno-18-24](#__codelineno-18-24)url = "https://download.pytorch.org/whl/xpu"
[#__codelineno-18-25](#__codelineno-18-25)explicit = true

```

## [Configuring accelerators with optional dependencies](#configuring-accelerators-with-optional-dependencies)

In some cases, you may want to use CPU-only builds in some cases, but CUDA-enabled builds in others, with the choice toggled by a user-provided extra (e.g., `uv sync --extra cpu `vs. `uv sync --extra cu128 `). 

With `tool.uv.sources `, you can use extra markers to specify the desired index for each enabled extra. For example, the following configuration would use PyTorch's CPU-only for `uv sync --extra cpu `and CUDA-enabled builds for `uv sync --extra cu128 `: 

```
[#__codelineno-19-1](#__codelineno-19-1)[project]
[#__codelineno-19-2](#__codelineno-19-2)name = "project"
[#__codelineno-19-3](#__codelineno-19-3)version = "0.1.0"
[#__codelineno-19-4](#__codelineno-19-4)requires-python = ">=3.14.0"
[#__codelineno-19-5](#__codelineno-19-5)dependencies = []
[#__codelineno-19-6](#__codelineno-19-6)
[#__codelineno-19-7](#__codelineno-19-7)[project.optional-dependencies]
[#__codelineno-19-8](#__codelineno-19-8)cpu = [
[#__codelineno-19-9](#__codelineno-19-9)  "torch>=2.9.1",
[#__codelineno-19-10](#__codelineno-19-10)  "torchvision>=0.24.1",
[#__codelineno-19-11](#__codelineno-19-11)]
[#__codelineno-19-12](#__codelineno-19-12)cu128 = [
[#__codelineno-19-13](#__codelineno-19-13)  "torch>=2.9.1",
[#__codelineno-19-14](#__codelineno-19-14)  "torchvision>=0.24.1",
[#__codelineno-19-15](#__codelineno-19-15)]
[#__codelineno-19-16](#__codelineno-19-16)
[#__codelineno-19-17](#__codelineno-19-17)[tool.uv]
[#__codelineno-19-18](#__codelineno-19-18)conflicts = [
[#__codelineno-19-19](#__codelineno-19-19)  [
[#__codelineno-19-20](#__codelineno-19-20)    { extra = "cpu" },
[#__codelineno-19-21](#__codelineno-19-21)    { extra = "cu128" },
[#__codelineno-19-22](#__codelineno-19-22)  ],
[#__codelineno-19-23](#__codelineno-19-23)]
[#__codelineno-19-24](#__codelineno-19-24)
[#__codelineno-19-25](#__codelineno-19-25)[tool.uv.sources]
[#__codelineno-19-26](#__codelineno-19-26)torch = [
[#__codelineno-19-27](#__codelineno-19-27)  { index = "pytorch-cpu", extra = "cpu" },
[#__codelineno-19-28](#__codelineno-19-28)  { index = "pytorch-cu128", extra = "cu128" },
[#__codelineno-19-29](#__codelineno-19-29)]
[#__codelineno-19-30](#__codelineno-19-30)torchvision = [
[#__codelineno-19-31](#__codelineno-19-31)  { index = "pytorch-cpu", extra = "cpu" },
[#__codelineno-19-32](#__codelineno-19-32)  { index = "pytorch-cu128", extra = "cu128" },
[#__codelineno-19-33](#__codelineno-19-33)]
[#__codelineno-19-34](#__codelineno-19-34)
[#__codelineno-19-35](#__codelineno-19-35)[[tool.uv.index]]
[#__codelineno-19-36](#__codelineno-19-36)name = "pytorch-cpu"
[#__codelineno-19-37](#__codelineno-19-37)url = "https://download.pytorch.org/whl/cpu"
[#__codelineno-19-38](#__codelineno-19-38)explicit = true
[#__codelineno-19-39](#__codelineno-19-39)
[#__codelineno-19-40](#__codelineno-19-40)[[tool.uv.index]]
[#__codelineno-19-41](#__codelineno-19-41)name = "pytorch-cu128"
[#__codelineno-19-42](#__codelineno-19-42)url = "https://download.pytorch.org/whl/cu128"
[#__codelineno-19-43](#__codelineno-19-43)explicit = true

```

!!! note "Note"

    Since GPU-accelerated builds aren't available on macOS, the above configuration will fail to install on macOS when the `cu128 `extra is enabled. 

## [The `uv pip `interface](#the-uv-pip-interface)

While the above examples are focused on uv's project interface ( `uv lock `, `uv sync `, `uv run `, etc.), PyTorch can also be installed via the `uv pip `interface. 

PyTorch itself offers a [dedicated interface](https://pytorch.org/get-started/locally/)to determine the appropriate pip command to run for a given target configuration. For example, you can install stable, CPU-only PyTorch on Linux with: 

```
[#__codelineno-20-1](#__codelineno-20-1)$ pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

```

To use the same workflow with uv, replace `pip3 `with `uv pip `: 

```
[#__codelineno-21-1](#__codelineno-21-1)$ uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

```

## [Automatic backend selection](#automatic-backend-selection)

uv supports automatic selection of the appropriate PyTorch index via the `--torch-backend=auto `command-line argument (or the `UV_TORCH_BACKEND=auto `environment variable), as in: 

```
[#__codelineno-22-1](#__codelineno-22-1)$ # With a command-line argument.
[#__codelineno-22-2](#__codelineno-22-2)$ uv pip install torch --torch-backend=auto
[#__codelineno-22-3](#__codelineno-22-3)
[#__codelineno-22-4](#__codelineno-22-4)$ # With an environment variable.
[#__codelineno-22-5](#__codelineno-22-5)$ UV_TORCH_BACKEND=auto uv pip install torch

```

When enabled, uv will query for the installed CUDA driver, AMD GPU versions, and Intel GPU presence, then use the most-compatible PyTorch index for all relevant packages (e.g., `torch `, `torchvision `, etc.). If no such GPU is found, uv will fall back to the CPU-only index. uv will continue to respect existing index configuration for any packages outside the PyTorch ecosystem. 

You can also select a specific backend (e.g., CUDA 12.8) with `--torch-backend=cu126 `(or `UV_TORCH_BACKEND=cu126 `): 

```
[#__codelineno-23-1](#__codelineno-23-1)$ # With a command-line argument.
[#__codelineno-23-2](#__codelineno-23-2)$ uv pip install torch torchvision --torch-backend=cu126
[#__codelineno-23-3](#__codelineno-23-3)
[#__codelineno-23-4](#__codelineno-23-4)$ # With an environment variable.
[#__codelineno-23-5](#__codelineno-23-5)$ UV_TORCH_BACKEND=cu128 uv pip install torch torchvision

```

At present, `--torch-backend `is only available in the `uv pip `interface. 

December 3, 2025
